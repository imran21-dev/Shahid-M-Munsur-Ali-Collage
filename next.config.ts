import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "i.postimg.cc",
      "www.aceternity.com",
      "avatar.vercel.sh",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
